import React from 'react';

import { Wrapper } from './styles';

interface Props {
  toggleTheme(): void;
}

const Home: React.FC<Props> = ({ toggleTheme }) => {
  return (
    <Wrapper>

    </Wrapper>
  );
};

export default Home;